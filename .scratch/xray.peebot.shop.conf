# 1. HTTP -> HTTPS Redirect for xray.peebot.shop
server {
    listen 80;
    listen [::]:80;
    server_name xray.peebot.shop;

    return 301 https://$host$request_uri;
}

# 2. HTTPS Server for xray.peebot.shop
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name xray.peebot.shop;

    # Wildcard SSL Certificates
    ssl_certificate /etc/letsencrypt/live/peebot.shop/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/peebot.shop/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    client_max_body_size 20M;

    # Reverse proxy to xray-proxy frontend container
    location / {
        proxy_pass http://127.0.0.1:8040;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
