# Issue tracker: Local Markdown

Issues and PRDs for this repo live as markdown files in `.scratch/`.

## Conventions

- One feature per directory: `.scratch/<feature-slug>/`
- The PRD is `.scratch/<feature-slug>/PRD.md`
- Implementation issues are `.scratch/<feature-slug>/issues/<NN>-<slug>.md`, numbered from `01`
- Triage state is recorded as a `Status:` line near the top of each issue file (see `docs/agents/triage-labels.md` for the role strings)
- When work on an issue is completed and verified, change its status to `Status: done`
- Comments and conversation history append to the bottom of the file under a `## Comments` heading

## When a skill says "publish to the issue tracker"

Create a new file under `.scratch/<feature-slug>/` (creating the directory if needed).

## When a skill says "fetch the relevant ticket"

Read the file at the referenced path. The user will normally pass the path or the issue number directly.

## Example Issue Format

```markdown
# 01-setup-proxy-core

Status: ready-for-agent

## Parent

Reference to PRD or parent issue (e.g. `.scratch/initial-setup/PRD.md`)

## What to build

Concise description of the vertical slice to implement.

## Acceptance criteria

- [ ] Criterion 1
- [ ] Criterion 2

## Blocked by

- None - can start immediately

## Comments

> *This was generated by AI during triage.*
```
